import React from "react";

const OgAnon = () => {
  const fetchData = async () => {
    var requestOptions = {
      method: "GET",
      redirect: "follow",
      accept: "application/json",
    };
  };
  fetchData();
  return <div>OgAnon</div>;
};

export default OgAnon;
